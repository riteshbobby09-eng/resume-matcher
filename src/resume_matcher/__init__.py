"""
Resume Matcher — Deterministic, explainable Resume-JD matching system.

Uses BGE embeddings for semantic representation and FAISS for vector retrieval,
with all business logic, scoring, and metadata in the Python application layer.
"""

__version__ = "0.1.0"
__app_name__ = "resume-matcher"
